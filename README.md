# CWEM Production Planner

A browser-based production planning board for scheduling colour-coded jobs across operators and working weeks.

The application stores schedule data in the current browser. Use **Backup** regularly and **Restore** to move a plan to another device.

## Development

```bash
npm install
npm run dev
```

Production builds are deployed automatically to GitHub Pages from `main`.
